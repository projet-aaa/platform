export const debug = true // are we in production or dev mode?
export const log = true // does the program produces logs?

export const domain = debug ? "localhost" : "jetpack0.trendio.fr"
export const port = 8080